{
  "name": "scope-interview-task",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm dev:backend\" \"pnpm dev:web\"",
    "dev:web": "pnpm --filter web dev",
    "dev:backend": "pnpm --filter backend dev",
    "build:shared": "pnpm --filter @scope/shared build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
